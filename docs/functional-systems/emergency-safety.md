# 紧急安全系统

**涉及子系统**：工控机（核心执行）、云端 API（事件上报/推送）、管理后台（告警展示/状态监控）  
**核心业务**：在地震、火灾等重大突发事件中，确保所有人员能立即离开健身房，不因门禁系统阻碍疏散

---

## 设计原则

> **安全第一，软件是辅助，硬件是底线。**

紧急安全系统必须遵循以下原则：

1. **硬件独立性**：核心疏散动作（开锁 + 报警）不得依赖工控机软件，必须有纯硬件电路兜底。
2. **失效安全（Fail-Safe）**：任何单点故障（断电、工控机宕机、网络中断）均应使系统趋向"安全状态"（门打开）。
3. **防误触设计**：紧急按钮必须有物理防护，防止日常误触；但紧急状态下操作必须直观、零培训可完成。
4. **不可静默失败**：报警触发后，必须有持续的声光输出，直到有权限人员主动复位。

---

## 硬件布局

### 门店物理布局

```mermaid
graph LR
    subgraph OUT["门店外部"]
        ENTRY["入口 / A门外侧"]
        OUT_ALARM["声光报警器 #3\n可选，提示外部人员"]
    end

    subgraph CHAMBER["隔离间"]
        A_DOOR["A 门\nfail-safe 电磁锁"]
        FACE_ID["海康刷脸终端\n人脸识别"]
        E_BTN1["紧急按钮 #1\n带玻璃罩 · 红色\n靠近 B 门侧墙面"]
        ALARM2["声光报警器 #2\n≥110dB + 红色频闪"]
        B_DOOR["B 门\nfail-safe 电磁锁"]
    end

    subgraph GYM["健身区"]
        E_BTN2["紧急按钮 #2\n带玻璃罩 · 红色\n靠近 B 门侧墙面"]
        ALARM1["声光报警器 #1\n≥110dB + 红色频闪\n天花板高处"]
        GYM_AREA["健身区域"]
    end

    subgraph CTR["控制箱（弱电间）"]
        PC["工控机"]
        E_RELAY["紧急回路继电器\n自锁型"]
        LOCK_RELAY["门锁供电继电器\n常闭触点串联"]
        KEY_SW["复位钥匙开关"]
        UPS["UPS 不间断电源"]
    end

    ENTRY -->|用户进入| A_DOOR
    A_DOOR -->|通过刷脸后开启| B_DOOR
    B_DOOR -->|进入| GYM_AREA

    E_BTN1 -. 信号线（红色） .-> CTR
    E_BTN2 -. 信号线（红色） .-> CTR
    ALARM1 -. UPS 独立供电 .-> CTR
    ALARM2 -. UPS 独立供电 .-> CTR
    A_DOOR -. 供电经串联继电器 .-> CTR
    B_DOOR -. 供电经串联继电器 .-> CTR
```

### 紧急按钮位置

| 编号 | 位置 | 面向人群 | 用途 |
|---|---|---|---|
| 紧急按钮 #1 | **隔离间内部**（靠近 B 门一侧墙面） | 已进入隔离间、无法通过刷脸出去的用户 | 地震/火灾时在隔离间内被困无法出去 |
| 紧急按钮 #2 | **健身区内部**（靠近 B 门的健身区墙面） | 健身区内的用户 | 健身区内发生紧急情况，无法正常走门禁出去 |

### 声光报警器位置

| 编号 | 位置 | 要求 |
|---|---|---|
| 报警器 #1 | 健身区内显眼位置（天花板或高处墙面） | 声压 ≥ 110dB，红色 LED 频闪 |
| 报警器 #2 | 隔离间内 | 同上，确保隔离间内人员能感知 |
| 报警器 #3 | 门店外部入口（可选） | 提示外部人员勿进入 |

---

## 硬件组成

| 物料名称 | 规格/要求 | 数量 | 说明 |
|---|---|---|---|
| 带玻璃罩紧急按钮 | 红色，带可击碎有机玻璃保护罩，内置玻璃破碎微动开关，IP54，24V DC | 2 | 隔离间内 + 健身区 B 门旁 |
| 声光报警器 | DC 12V/24V，≥110dB，红色频闪，独立常闭/常开输入接口 | 2~3 | 健身区 + 隔离间（+ 门店外） |
| 紧急回路继电器模块 | 双路，DC 12V，带自锁功能（触发后保持，需手动/远程复位） | 1 | 安装在控制箱内，纯硬件报警回路核心 |
| 复位钥匙开关 | 带锁芯，仅管理员持有钥匙，旋转复位 | 1 | 安装在控制箱内或专用位置，用于解除报警 |

---

## 电路设计

### 两阶段触发机制

紧急按钮分两个物理阶段触发，对应两套独立电路：

```mermaid
graph TB
    subgraph DEVICE["紧急按钮装置（带玻璃罩）"]
        GLASS["有机玻璃罩\n物理防误触屏障"]
        GLASS_SW["玻璃破碎微动开关\n常闭 NC — 玻璃完好时导通"]
        BTN["红色紧急按钮\n玻璃破碎后可按下"]
    end

    GLASS -- "击碎玻璃" --> GLASS_SW
    GLASS -- "玻璃碎后可触及" --> BTN

    GLASS_SW -- "阶段 1\n玻璃破碎瞬间触发" --> HW["硬件报警回路\n自锁继电器直接驱动\n不经过任何软件"]
    BTN -- "阶段 2\n按下按钮触发" --> SW["工控机 GPIO 输入\n软件开门 + 事件上报"]
    BTN -- "阶段 2\n同步触发（并行）" --> HW_DOOR["门锁供电串联继电器\n硬件直接切断电磁锁供电"]

    HW --> SIREN["声光报警器\n持续鸣响 ≥110dB\n红色频闪"]
    HW_DOOR --> DOORS["AB 门同时弹开\nfail-safe 断电即开\n响应 ≤100ms"]
    SW --> DOORS
    SW --> CLOUD["云端上报\n推送通知至总部"]
```

### 阶段 1：玻璃破碎 → 硬件报警（无需软件介入）

玻璃罩内置**常闭型微动开关**，玻璃完好时电路导通；击碎玻璃后，玻璃碎片释放弹片，微动开关断开，触发自锁继电器，直接接通声光报警器电源。

```mermaid
graph LR
    PWR["DC 12V\n电源（UPS供电）"]

    PWR -->|"①正常时导通"| SW["玻璃破碎微动开关\n常闭 NC"]
    SW -->|"②触发线圈"| LATCH_COIL["自锁继电器\n线圈"]
    LATCH_COIL -->|"③触点闭合并自锁"| LATCH_CON["自锁继电器\n触点（常开→闭合）"]
    PWR -->|"④独立供电"| LATCH_CON
    LATCH_CON -->|"⑤接通报警器"| SIREN["声光报警器\n持续输出"]

    BREAK(["击碎玻璃\n⚡ SW 断开"])
    BREAK -. "玻璃破碎\n线圈断电" .-> LATCH_COIL

    KEY(["钥匙开关复位\n✋ 管理员操作"])
    KEY -. "强制断开\n自锁触点" .-> LATCH_CON
```

**关键特性**：
- 自锁继电器一旦触发，即使玻璃重新放回（开关恢复）也**不会自动复位**
- 只有通过**复位钥匙开关**才能解除报警，防止任意人员关闭报警

工控机同时通过 GPIO 检测破碎传感器状态，用于事件上报（软件层面的信息通道，不作为报警触发依据）。

### 阶段 2：按钮按下 → 所有门全开

按钮信号通过两条并行路径执行开门动作，**任意一条路径成功即可开门**：

#### 路径 A：硬件直接切断门锁电源（优先，不依赖软件）

```mermaid
graph LR
    PWR2["DC 12V 电源"]
    LOCK_RELAY["门锁供电串联继电器\n常闭触点 · 正常时导通"]
    A_LOCK["A 门电磁锁\nfail-safe 常开型"]
    B_LOCK["B 门电磁锁\nfail-safe 常开型"]
    BTN2(["紧急按钮\n按下"])

    PWR2 -->|"① 正常时电流通过"| LOCK_RELAY
    LOCK_RELAY -->|"② 供电"| A_LOCK
    LOCK_RELAY -->|"② 供电"| B_LOCK

    BTN2 -->|"③ 线圈得电\n常闭触点断开"| LOCK_RELAY
    LOCK_RELAY -- "④ 断电" --> A_LOCK
    LOCK_RELAY -- "④ 断电" --> B_LOCK
    A_LOCK -->|"⑤ 弹开 ≤100ms"| OPEN_A["A 门打开"]
    B_LOCK -->|"⑤ 弹开 ≤100ms"| OPEN_B["B 门打开"]
```

> 电磁锁已采购 **fail-safe 常开型**（见 BOM 清单第 4、5 项），断电即自动弹开，无需任何软件指令。

#### 路径 B：工控机软件开门（辅助，用于日志记录）

```mermaid
graph TD
    GPIO_IN["工控机 GPIO 输入\n检测到紧急按钮高电平"]

    GPIO_IN --> UNLOCK_A["GPIO 输出\n解锁 A 门"]
    GPIO_IN --> UNLOCK_B["GPIO 输出\n解锁 B 门"]
    GPIO_IN --> REPORT["MQTT 上报\nemergency_triggered"]
    GPIO_IN --> HOLD["持续保持解锁状态\n直到收到 emergency_reset 指令"]

    REPORT --> CLOUD_NOTIFY["云端推送\nApp + 短信 → 总部"]
```

---

## 完整触发流程

```mermaid
flowchart TD
    START(["👤 用户发现紧急情况\n地震 / 火灾 / 被困"])

    START --> BREAK["击碎玻璃罩"]

    BREAK --> HW_ALARM["🔴 硬件报警回路触发\n声光报警器持续鸣响\n≥110dB + 红色频闪\n（纯硬件，不经过软件）"]
    BREAK --> GPIO_GLASS["工控机 GPIO 检测到\n玻璃破碎信号"]

    GPIO_GLASS --> REPORT_GLASS["MQTT 上报\nemergency_glass_broken\n→ 管理后台弹窗告警"]

    HW_ALARM --> PRESS["按下玻璃罩内的\n红色紧急按钮"]
    REPORT_GLASS --> PRESS

    PRESS --> PATH_A["路径 A（硬件）\n继电器切断\n电磁锁供电"]
    PRESS --> PATH_B["路径 B（软件）\n工控机 GPIO\n检测按钮信号"]

    PATH_A --> DOORS_OPEN["A 门 + B 门\n同时弹开\n≤100ms"]
    PATH_B --> DOORS_OPEN
    PATH_B --> REPORT_EMRG["MQTT 上报\nemergency_triggered"]

    REPORT_EMRG --> NOTIFY["云端推送通知\nApp 高优先级推送\n短信（兜底）\n飞书机器人（可选）\n→ 总部值班人员"]

    DOORS_OPEN --> EVACUATE["🚶 人员疏散\n门持续开启\n报警持续鸣响"]

    EVACUATE --> ADMIN["👷 管理员到场"]

    ADMIN --> RESET_HW["用钥匙开关\n复位硬件报警回路\n关闭声光报警器"]
    RESET_HW --> RESET_SW["管理后台确认\n事件处理完毕\n下发 emergency_reset"]
    RESET_SW --> NORMAL(["✅ 系统恢复\n正常门禁逻辑"])
```

---

## 云端事件上报

工控机通过 MQTT 上报以下紧急事件：

| 事件类型 | 触发时机 | 上报内容 | 云端动作 |
|---|---|---|---|
| `emergency_glass_broken` | 玻璃破碎传感器触发 | 按钮编号、时间戳、门店 ID | 推送告警通知 |
| `emergency_triggered` | 紧急按钮按下 | 按钮编号、时间戳、门店 ID | 推送紧急通知至总部（短信 + App 推送） |
| `emergency_doors_opened` | 所有门解锁完成 | 门列表、时间戳 | 记录日志 |
| `emergency_reset` | 管理员复位操作 | 操作人账号、时间戳 | 关闭告警、恢复正常门禁逻辑 |

### MQTT Payload 示例

```json
{
  "event": "emergency_triggered",
  "store_id": "store_001",
  "button_id": "emergency_btn_chamber",
  "location": "隔离间内",
  "timestamp": "2025-03-13T10:23:45+08:00",
  "doors_unlocked": ["A", "B"]
}
```

---

## 总部通知机制

| 通知渠道 | 触发事件 | 接收方 | 内容 |
|---|---|---|---|
| App 推送（高优先级） | `emergency_triggered` | 总部运营值班人员 | 门店名称、触发位置、时间 |
| 短信 | `emergency_triggered` | 总部紧急联系人（最多 3 人） | 同上，确保无 App 也能收到 |
| 管理后台弹窗 | `emergency_glass_broken` / `emergency_triggered` | 当前登录的所有管理后台用户 | 高亮告警弹窗，要求确认 |
| 飞书机器人（可选） | `emergency_triggered` | 运营群 | 自动发送告警消息 |

> **短信为兜底通知渠道**，必须实现，确保 App 通知失效（推送服务故障）时仍能通知到人。

---

## 系统状态机

紧急安全系统有四个状态：

```mermaid
stateDiagram-v2
    [*] --> STANDBY : 系统启动

    STANDBY --> ALARMING : 玻璃破碎\n声光报警器启动

    ALARMING --> EVACUATING : 紧急按钮按下\nAB 门全部解锁\n上报总部

    EVACUATING --> RESETTING : 管理员到场\n钥匙开关复位\n关闭报警器

    RESETTING --> STANDBY : 管理后台确认\n事件处理完毕\n系统恢复正常

    note right of STANDBY
        门锁：由门禁系统控制
        报警器：静默
        可接受：正常刷脸 / 远程控制
    end note

    note right of ALARMING
        门锁：仍由门禁系统控制
        报警器：持续鸣响 + 频闪
        可接受：按下按钮进入疏散
    end note

    note right of EVACUATING
        门锁：全部强制解锁，保持开启
        报警器：持续鸣响 + 频闪
        可接受：等待管理员到场
    end note

    note right of RESETTING
        门锁：仍保持解锁，直到确认安全
        报警器：管理员可关闭
        可接受：管理后台确认处理完毕
    end note
```

| 状态 | 描述 | 门锁状态 | 报警器 | 可接受的操作 |
|---|---|---|---|---|
| `STANDBY` | 正常运行 | 由门禁系统控制 | 静默 | 正常刷脸、远程控制 |
| `ALARMING` | 玻璃已破碎，等待按钮 | 由门禁系统控制（尚未开门） | 持续鸣响 + 频闪 | 按下按钮进入疏散 |
| `EVACUATING` | 紧急按钮已按下 | **全部强制解锁，保持开启** | 持续鸣响 + 频闪 | 管理员到场处理 |
| `RESETTING` | 管理员到场，正在复位 | **仍保持解锁**，直到确认安全 | 可由管理员关闭 | 管理后台确认完毕后恢复 STANDBY |

---

## 防误触与防滥用设计

| 风险 | 设计应对 |
|---|---|
| 普通用户好奇心误触按钮 | 玻璃罩需主动击碎才能按到按钮；玻璃破碎即触发报警，有明显代价 |
| 恶意触发（骚扰报警） | 玻璃破碎传感器记录时间戳上报云端；管理后台可查历史记录；门店监控录像可回溯 |
| 报警器被人为遮盖 | 声光报警器安装在高处（≥2.5m），普通人无法轻易遮盖 |
| 复位钥匙丢失 | 复位钥匙建议备份 2 把，一把现场、一把总部；管理后台也提供远程复位指令 |
| 工控机宕机时按钮无效 | 路径A（硬件直接切断门锁供电）不经过工控机，确保兜底 |
| 断电时系统失效 | 电磁锁为 fail-safe 常开型，断电即自动弹开；UPS 为工控机和报警器供电 |

---

## 与其他系统的联动

| 联动系统 | 触发动作 | 说明 |
|---|---|---|
| 门禁系统 | 紧急状态下强制解锁并锁定门禁控制逻辑（禁止其他指令关门） | 防止系统在紧急状态下自动关门 |
| 硬件控制系统 | 触发所有灯光全亮（含健身区、隔离间、走道） | 确保疏散路径明亮可见 |
| 数据分析系统 | 紧急事件记录入日志，纳入安全报表 | 可用于事后复盘 |
| 淋浴系统 | 触发后关闭所有淋浴热水（防止无人监管时漏水） | 可选联动 |

---

## BOM 补充清单

以下硬件需在现有 BOM 基础上新增：

| 序号 | 物料名称 | 规格/要求 | 数量 | 状态 | 备注 |
|---|---|---|---|---|---|
| E-1 | 带玻璃罩紧急按钮 | 红色，内置玻璃破碎微动开关（常闭型），IP54，24V DC，面板嵌入式安装 | 2 | 🔄 | 隔离间 + 健身区 B 门旁各 1 |
| E-2 | 声光报警器 | DC 12V，≥110dB，红色 LED 频闪，常闭/常开输入 | 2~3 | 🔄 | 健身区 + 隔离间（+ 门店外可选）|
| E-3 | 自锁继电器模块 | DC 12V 线圈，带自锁功能（需断电或外部信号复位），DIN 导轨安装 | 1 | 🔄 | 安装在控制箱内 |
| E-4 | 门锁供电串联继电器 | DC 12V，常闭触点，用于紧急状态切断 AB 门电磁锁供电 | 1 | 🔄 | 安装在控制箱内，串联在门锁供电回路 |
| E-5 | 复位钥匙开关 | 带锁芯，双位（ON/OFF），面板安装 | 1（备用 1） | 🔄 | 主锁安装在控制箱面板；备用钥匙交总部保管 |

---

## 安装与接线要点

1. **紧急按钮接线**：玻璃破碎微动开关（常闭）串联在自锁继电器线圈回路中；按钮（常开）接入工控机 GPIO 输入 + 门锁供电串联继电器控制线圈。

2. **门锁供电路径**：

```mermaid
graph LR
    PS["开关电源\nDC 12V"] --> RELAY_NC["门锁供电串联继电器\n常闭触点\n正常状态：导通"]
    RELAY_NC --> A_LOCK2["A 门电磁锁"]
    RELAY_NC --> B_LOCK2["B 门电磁锁"]

    BTN_PRESS(["紧急按钮按下"]) -->|"线圈得电"| RELAY_NC
    RELAY_NC -. "常闭触点断开\n门锁断电弹开" .-> A_LOCK2
    RELAY_NC -. "常闭触点断开\n门锁断电弹开" .-> B_LOCK2
```

3. **报警器独立供电**：声光报警器通过 UPS 供电，不与工控机同一路，确保工控机宕机时报警器仍能正常运行。

4. **所有紧急回路走线**：使用红色线缆，与普通控制信号线区分，便于维护排查。

---

## 定期维护检查

| 检查项 | 频率 | 方法 |
|---|---|---|
| 玻璃罩完好性检查 | 每月 | 目视检查，确认玻璃未裂纹或松动 |
| 报警器功能测试 | 每季度 | 在非营业时间用备用玻璃片替换测试，确认声光正常 |
| 复位钥匙可用性 | 每季度 | 确认钥匙可正常旋转复位 |
| 工控机 GPIO 信号验证 | 每季度 | 通过管理后台触发测试模式，验证信号上报是否正常 |
| 门锁断电开门测试 | 每半年 | 短暂断开门锁供电，确认 AB 门均能弹开 |

---

## 待确认事项

- [ ] 紧急按钮具体型号：确认玻璃破碎微动开关规格（常闭 NC 接点电压/电流）
- [ ] 声光报警器数量：门店外是否需要第 3 个报警器
- [ ] 复位方式：管理后台远程复位是否作为主要复位手段（减少对到场人员的依赖）
- [ ] 总部短信通知服务商：确认短信 API 供应商（阿里云 / 腾讯云等）
- [ ] 紧急状态下灯光全亮联动：是否需要覆盖灯光手动关闭的状态
- [ ] 自锁继电器与 UPS 供电范围：确认 UPS 容量是否覆盖报警器持续运行（见 BOM E-2）
