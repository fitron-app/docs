---
description: 无人值守连锁健身房系统——项目背景、技术栈、开发规范
alwaysApply: true
---

# 项目背景

本项目是一套**无人值守连锁健身房服务系统**，目标是支撑全国百店至千店规模的无人健身房日常运营。

## 核心场景

- 会员通过微信小程序购买会员资格/体验卡，刷脸进出健身房
- 门店内无人值守，所有门禁、灯光、淋浴等硬件由工控机自动控制
- 运营人员通过管理后台 Web 进行门店管理、会员管理、数据分析
- 经营数据同步至飞书多维表格，供管理层查看

## 五端子系统

| 子系统 | 技术栈 | 部署位置 |
|--------|-------|---------|
| 工控机（IPC） | Python 3.x | 门店本地，RK3576 工控机 |
| 云端 API | Kotlin + Spring Boot | 腾讯云（南京 HA） |
| 管理后台 Web | Vue 3 + TypeScript | 腾讯云（静态托管） |
| 微信小程序 | uni-app | 微信平台 |
| 飞书多维表格 | 飞书开放 API | 飞书平台 |

## 工控机硬件

- **芯片**：RK3576（ARM，Linux）
- **外设**：GPIO 继电器（门锁/灯光）、门磁传感器、人脸识别摄像头（USB/CSI）、UPS 通信（RS485）
- **网络**：有线以太网优先，4G 备用
- **运行环境**：Python APP，由 systemd 管理，无人值守 7×24 运行

## 关键约束

- 门店无专业运维，所有故障必须支持**远程修复或自动恢复**
- 网络环境参差不齐（一线城市光纤 / 小城市宽带 / 4G 专线）
- 人脸识别和门禁操作有实时性要求，网络断线时必须**本地降级运行**
- 工控机升级使用 **A/B 包 OTA**，看门狗三级自愈，远程运维通过 **FRP 穿透 SSH**

## 文档站

本仓库是 **VitePress 文档站**（`npm run docs:dev`），所有规划、架构、技术方案均以 Markdown 编写，部署到 Cloudflare Pages。

文档目录结构：
- `docs/subsystems/` — 子系统边界与接口
- `docs/functional-systems/` — 业务功能设计
- `docs/technical-solutions/` — 技术方案专题
- `docs/hardware/` — 硬件 BOM 与接线
- `docs/planning/` — WBS 与工期
- `docs/devops/` — 运维与 CI/CD
- `docs/hiring/` — 招聘 JD 与面试题

---

# 开发规范

## 文档规范

- 每个文档顶部注明**适用子系统**和**核心目标**
- 使用 Mermaid 图表描述流程（支持 `sequenceDiagram`、`graph TD`、`flowchart TD`）
- 待确认事项统一放在文档末尾 `## 待确认事项` 区块，用 `- [ ]` 格式
- 不写无意义的示例文件，不写 README（文档即文档）

## 代码规范（工控机 Python）

- 文件名和类名一致，一个文件一个类
- 不保留无意义的函数和文件
- 不添加仅复述代码行为的注释，注释只解释意图和约束

## 沟通规范

- 不了解业务逻辑时，主动询问，不猜测
- 不考虑新旧版本兼容，保持代码整洁
- 回复使用中文，git commit message 使用中文
